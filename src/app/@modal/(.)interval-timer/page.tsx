import { IntervalTimerModal } from "./modal";
import { IntervalTimerForModal } from "~/components/interval-timer";

export default function IntervalTimerModalPage() {
  return (
    <IntervalTimerModal>
      <IntervalTimerForModal />
    </IntervalTimerModal>
  );
}
